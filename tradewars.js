// hide inactive forms, show the active one, gray out the unselected buttons and blue the selected one
function setActiveForm(targetString)
{/*
	// hide/grey everything
	$(".entryForm").hide();
	$(".navItem").css("background-color", "gray");
	
	// show/blue the two you're using
	if(targetString == 'Exchange')
	{
		$("#ExchangeForm").show();
		$("#NavExchange").css("background-color", "blue");
	}
	else if(targetString == 'Add')
	{
		$("#AddForm").show();
		$("#NavAdd").css("background-color", "blue");
	}
	else if(targetString == 'Update')
	{
		$("#UpdateForm").show();
		$("#NavUpdate").css("background-color", "blue");
	}*/
}